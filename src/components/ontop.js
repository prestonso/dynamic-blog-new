import React from 'react'
import { Link } from 'gatsby'

class OnTop extends React.Component {
  render() {
    return (
            <section id="get-in-touch">
            <div className="container">
                <div className="section-header">
                <h1 className="section-title text-center wow fadeInDown animated" style={{visibility: 'visible', animationName: 'fadeInDown'}}>FRUGALTEST BLOG</h1>
                <h2 className="text-center wow fadeInDown animated" style={{visibility: 'visible', animationName: 'fadeInDown'}}>Explore more on Load Testing with us!</h2>
                </div>
            </div>
            </section>
 )
}
}
export default OnTop